Program used in an Arduino Board in order to control a microfluidic testing platform, composed of sensors and actuators, which can be configured and accessed through the USB using tha Modbus protocol.